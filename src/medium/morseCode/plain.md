# Plain English Solution
```
Having an object with letters converted in morse code, iterate trough the object to have my word coverted into morse code.
Having an function that swap my keys and value into the object, returning the object converted and using it to convert my morse code into an english word.
```